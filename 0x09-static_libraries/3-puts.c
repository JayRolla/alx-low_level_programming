#include "main.h"
#include <stdio.h> // Include <stdio.h> here

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    _puts("\"Programming is like building a multilingual puzzle");
    return (0);
}
